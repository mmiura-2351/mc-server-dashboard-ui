# ==============================================
# MC Server Dashboard 環境設定ファイル
# このファイルを .env.local にコピーして使用してください
# ==============================================

# API URL設定
# ローカル開発: http://localhost:8000
# 本番環境(nginx経由): https://your.domain.com
NEXT_PUBLIC_API_URL=http://localhost:8000

# アプリケーション設定（オプション）
NEXT_PUBLIC_APP_NAME=MC Server Dashboard
NEXT_PUBLIC_DEFAULT_LANGUAGE=en

# 環境設定
# development: 開発環境（APIプロキシ有効）
# production: 本番環境（APIプロキシ無効、nginxが処理）
NODE_ENV=development

# ==============================================
# 注意事項
# ==============================================
# 
# 1. ローカル開発環境の場合:
#    - NEXT_PUBLIC_API_URL は http://localhost:8000 のままでOK
#    - NODE_ENV は development に設定
#    - APIプロキシが自動的に有効になります
#
# 2. 本番環境（nginx経由）の場合:
#    - NEXT_PUBLIC_API_URL を https://your.domain.com に変更
#    - NODE_ENV を production に設定
#    - nginxでAPIプロキシ設定が必要です（/api/* → バックエンド）
#
# 3. Mixed Content エラーが発生する場合:
#    - HTTPSサイトからHTTP APIにアクセスできません
#    - nginx設定でAPIもHTTPS経由でプロキシしてください
#    - 詳細は nginx-configuration-guide.md を参照